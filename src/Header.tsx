import "./Header.css"

function Header() {
    return (
        <header>
            <button>Show sidebar</button>
            <input type="search" placeholder="Search"></input>
        </header>
    )
}

export default Header
